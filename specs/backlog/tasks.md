# Backlog — minted, not yet picked up

Anointed backlog: each open item below carries a registry ID whose only carrier
is this TODO. Minting into backlog is deliberate — the TODO proves intent and
names who is carrying what. Moving an item out of backlog means creating its
feature spec and tasks, which claims the ID and retires the line here.

- [ ] T900 Deliver `HomeDisplayName.normalized(_:)` natively in HomesFlow (whitespace trim + collapse; the Clewseau trial slice, delivered here as well) — **Traces**: US-CLEW-01, FR-CLEW-01, AC-CLEW-01
